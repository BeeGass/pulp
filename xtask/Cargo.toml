[package]
name = "xtask"
version = "0.1.0"
edition = "2024"
publish = false
license = "MIT"
description = "Per-machine / per-OS pulp build operator"

[[bin]]
name = "xtask"
path = "src/main.rs"

[dependencies]
anyhow = "1.0.104"
clap = { version = "4.6.7", features = ["derive"] }
